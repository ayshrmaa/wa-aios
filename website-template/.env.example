# Server-side only. Never prefix this with NEXT_PUBLIC_ or expose it in client code.
RETELL_API_KEY=

# Optional: the verified demo agent is used by default when this is omitted.
RETELL_AGENT_ID=agent_9fc5538faca03db216cac1fa4b
